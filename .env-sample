#Environment
NODE_ENV="dev"

#Database
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dangout
DB_USER=postgres
DB_PASS=changeme
POSTGRES_PASSWORD=changeme

#SMTP Server
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=changeme
SMTP_PASS=changeme

#Token Salt
TOKEN_SECRET=changeme

